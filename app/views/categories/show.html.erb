<div class="row">

	<div class="col-md-4">

	</div>

	<div class="col-md-8">

		<div class="rest-info">

			<h2 class="rest-title"><%= @category.name %></h2>
			<%= link_to 'Edit', edit_category_path(@category) %> |
			<%= link_to 'Delete', @category, method: :delete, data: { confirm: 'Are you sure?' } %>
			<%= link_to 'Back', categories_path %>
		</div>
	</div>
</div>
