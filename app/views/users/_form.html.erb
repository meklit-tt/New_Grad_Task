<%= form_for @user, html: { multipart: true } do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= restaurant.errors.count %> are the errors.</h2>
      <ul>
      <% retaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
